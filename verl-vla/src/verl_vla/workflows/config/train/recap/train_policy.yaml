# Copyright 2026 Bytedance Ltd. and/or its affiliates
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# ReCap policy-training stage.

# Hydra configuration composition order.
defaults:

  # Base actor-only SFT trainer config.
  - /base/sft@_here_

  # Apply this stage config last.
  - _self_


# Whether to train the final RECAP policy.
enable: true

# Dataset used when this stage runs without fresh collected datasets.
dataset:

  # Root directory used by this component.
  root: /tmp/verl_vla_lerobot_records/local/verl_vla_libero_collected

  # LeRobot dataset repository identifier.
  repo_id: local/verl_vla_libero_collected


# Policy dataloader settings.
data:

  # Number of future low-level actions used to train the policy action chunk.
  action_delta_steps: 50


# Policy TrainCluster settings.
cluster:

  # Checkpoint lifecycle configuration.
  checkpoint:

    # Default directory used for local checkpoints.
    default_local_dir: checkpoints/${recap.train_policy.trainer.project_name}/${recap.train_policy.trainer.experiment_name}

  # Shared actor, rollout, and reference-policy configuration.
  actor_rollout_ref:

    # Field names exchanged at the DataProto boundary.
    data_keys:

      # Dataset field identifying intervention samples.
      indicator: recap.indicator

    # Actor training configuration.
    actor:

      # Checkpoint lifecycle configuration.
      checkpoint:

        # Training-state components written to each checkpoint.
        save_contents: ["model", "optimizer", "extra", "hf_model"]

        # Training-state components restored from a checkpoint.
        load_contents: ["model", "optimizer", "extra"]

      # Advantage-conditioned policy configuration.
      acp:

        # Whether this component is enabled.
        enable: true

      # Optimizer configuration.
      optim:

        # Optimizer learning rate.
        lr: 1e-4


# Policy trainer settings.
trainer:

  # Tracking project name.
  project_name: recap

  # Tracking name for this experiment.
  experiment_name: policy

  # Metric logging backends.
  logger: ["console", "tensorboard"]

  # Number of full policy-dataset passes per training stage.
  total_epochs: 5

  # Whether checkpoint resume restores dataloader progress.
  resume_dataloader_state: false
